<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Bluetooth. The runtime pair (SCAN/CONNECT) is API 31+; the older
         install-time pair covers 26–30. SCAN is flagged neverForLocation so
         Android 12+ doesn't fold the Bluetooth ask into the location ask — the
         two are separate screens in the tutorial and must stay separate. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- Location: the blue dot, the GPS warm-start seed we hand the device, and
         the backup fix while the head unit has none. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Only while a ride is recording: then the pocketed phone stays a seed and
         a backup track for the head unit, which is exactly what Android wants a
         location-typed foreground service for. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Only to scan a mesh channel's QR invite, and only while that scanner is
         on screen. Not required: the link can be pasted instead. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <!-- Declaring CAMERA implies required="true" unless this says otherwise, and
         a missing camera only costs the QR scanner, not the app. -->
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <application
        android:name=".OpenTrailPaperApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OpenTrailPaper">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:label="@string/app_name"
            android:theme="@style/Theme.OpenTrailPaper"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Opening a .gpx from Files, mail, Komoot or Strava.

                 GPX is served as gpx+xml, octet-stream, text/xml or nothing at
                 all depending on the provider, so the exact type alone is not
                 enough: on a real device 7 of 8 .gpx files came back as
                 application/octet-stream, and only a Suunto export as
                 application/gpx+xml. Hence the second filter.

                 The pathPattern on it does NOT narrow anything. Android ignores
                 path matching once an intent carries a MIME type, so that filter
                 is in practice "any octet-stream file" and this app is offered
                 for .bin and .jpg too — verified with `cmd package
                 query-activities`. The pattern is kept only for the type-less
                 case. What actually decides is the file's first bytes, in
                 RouteImport, which rejects a non-GPX with a readable message.
                 Dropping the filter would trade that chooser noise for losing
                 open-with on almost every real .gpx. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" android:mimeType="application/gpx+xml" />
                <data android:scheme="file" android:mimeType="application/gpx+xml" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data
                    android:scheme="content"
                    android:mimeType="application/octet-stream"
                    android:pathPattern=".*\\.gpx" />
                <data
                    android:scheme="file"
                    android:mimeType="application/octet-stream"
                    android:pathPattern=".*\\.gpx" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/gpx+xml" />
            </intent-filter>
        </activity>

        <service
            android:name=".ble.RideLocationService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Empty on purpose: it exists because getActiveSessions() — how the
             device's MUSIC page learns what the phone is playing — is gated on
             the rider granting notification access, and that grant is bound to
             a NotificationListenerService. Nothing here reads notifications. -->
        <service
            android:name=".ble.MediaListener"
            android:exported="true"
            android:label="OpenTrailPaper media"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Shares downloaded .fit rides and device logs with other apps
             (Strava, Files, mail) the way iOS hands out a file URL. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
